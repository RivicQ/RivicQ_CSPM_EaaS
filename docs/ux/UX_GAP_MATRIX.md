# UX gap matrix

| Journey | Today | Gap | Priority |
|---------|-------|-----|----------|
| New customer → first scan → inbox | Register/login, scanner, dashboard empty state | No org wizard; Pages has no API | P1 |
| Analyst: critical → evidence → asset → status | `/findings` + asset query | No persist status/assign | P1 |
| CISO: overview → quantum → compliance → report | Persona layout + analytics | Board PDF builder missing | P2 |
| PQC engineer: CBOM → RSA → roadmap | `/bom`, `/migration` | No application-level progress % unless engine provides it | P1 |
| DevSecOps: CI → gate → rescan | CLI + GitHub Action + PATH tools | In-app CI run history not stored | P2 |
| Admin: users → SSO → audit | `/admin` | SSO ACS not live; audit no export | P1 |
| Connect cloud | Enterprise pages | Credentials + empty connectors | P1 |
| Global search | ⌘K palette | No server search index | P2 |
