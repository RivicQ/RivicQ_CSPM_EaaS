# PQC UX audit

`/migration` uses latest scan intelligence / QBOM. Qiskit estate scores are a local taxonomy (`qiskitprofile`), not IBM Quantum hardware.

RSA-2048 is classified. No invented “classical 80% / quantum 20%” bars unless the engine returns those fields.

Persona CISO sees PQC via Overview + Migration; engineers use Scanner + PATH tools.
