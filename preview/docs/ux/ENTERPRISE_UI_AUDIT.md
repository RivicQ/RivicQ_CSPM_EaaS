# Enterprise UI audit

Shell: persistent sidebar, collapse, mobile bottom nav (Overview / Findings / Scans / Assets), breadcrumbs, ⌘K palette, help drawer (`?`), persona (CISO / Analyst / Engineer), edition selector (preference, not license), API reachable status chip.

Missing vs full enterprise shell: org/tenant picker, keyboard shortcut overlay, environment staging/prod badge beyond API/demo, shared notification preferences.

Pages remain a static demo. Do not present them as a live estate.
