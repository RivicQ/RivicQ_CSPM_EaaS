# Datasets

See [DATASETS.md](../DATASETS.md) at the repository root for how to add, validate, and license samples.

This directory holds `expected.json` pointers. Weak-crypto files live in `fixtures/` so the Go module and CLI skip rules stay consistent.
