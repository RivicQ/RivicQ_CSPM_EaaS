/** Honest copy for the public GitHub Pages front door. No secrets. */

export const PUBLIC_ENGINE_SCAN_COPY =
  'Live scanning runs against the RivicQ CBOM engine. On the public site the engine isn’t reachable — sign in to run a real scan and get a full, evidence-backed report. We never show fabricated findings here.';

export const PUBLIC_ENGINE_IBM_HONESTY =
  'No fabricated co-sell records. Marketplace metadata is not auto-published. Qiskit scores on scans are a local taxonomy — not IBM Quantum hardware.';

export const PUBLIC_ENGINE_IBM_PAGES =
  'GitHub Pages cannot load live readiness state. Run the API to see the checklist.';

export const PUBLIC_ENGINE_IBM_COPY = `${PUBLIC_ENGINE_IBM_HONESTY} ${PUBLIC_ENGINE_IBM_PAGES}`;

export const PUBLIC_ENGINE_CHIP_DISCONNECTED = 'Needs the RivicQ engine';
export const PUBLIC_ENGINE_CHIP_LIVE = 'RivicQ engine · live';
